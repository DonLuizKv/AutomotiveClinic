export default function Header() {
    return (
        <header>
            <span></span>
            <nav>
                <button>notifications</button>
                <button>user</button>
            </nav>
        </header>
    );
}